-- =============================================================
-- V4: Admin user placeholder
-- The admin user is created at startup by AdminInitializer
-- using the ADMIN_PASSWORD env var (default: admin123).
-- This migration exists to reserve the version slot.
-- =============================================================
SELECT 1;
